если захочешь что-либо изменить, то сначала нужно будет запушить изменения в ветку master, а потом вызвать npm run deploy.

npm run deploy запускает скрипты gh-pages, которые переносят файлы проекта из ветки master в ветку gh-pages, выкладывая проект в сеть.